#ifndef __VOLUME_H_
#define __VOLUME_H_

/*
@parameter a length of the cube
@returns volume of the cube, if parameter is negative funtion returns 0.0
*/
double cube_volume(double a);

/*
@parameter r radius of the sphere
@returns volume of the sphere, if parameter is negative function returns 0.0
*/
double sphere_volume(double r);

#endif // __VOLUME_H_